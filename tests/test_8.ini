; 
;  Real connection test
;
[Basic fonctionnal configuration left ] 
	source = system:capture_1
	destination = system:playback_1

[Basic fonctionnal configuration right ] 
	source = system:capture_2 
	destination = system:playback_2

